import {Simulator} from "../rwth.visualisation/Simulator";

describe('The simulator test', () => {

    let car = new Simulator();

    it('should return ...', () => {
        
        expect(1).toEqual(1);
    });
});